# Git initialize
git init

# Status check
git status

# Username aur email set karo
git config user.email "012345nirmal@gmail.com"
git remote add origin https://github.com/Kushagra-nirmal/fullstack.git

# GitHub repository connect karo
git remote add origin https://github.com/Kushagra-nirmal/MERN.git

# Config check
git config --list

# Remote check
git remote -v

# Files stage karo
git add .

# Status check
git status

# Commit karo
git commit -m "Initial commit"

# Branch ko main rename karo
git branch -M main

# GitHub par push karo
git push -u origin main

# if delete repository code file use command
git rm -r --cached .
git commit -m "Remove files from repository"
git push origin main

git add "Day2"
git commit -m "Add Day2"
git push origin main